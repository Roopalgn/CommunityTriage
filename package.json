{
  "name": "community-triage",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered community needs triage and volunteer coordination dashboard for NGOs, built with Google Gemini.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "evaluate": "node evaluation/run-evaluation.js",
    "evaluate:offline": "node evaluation/run-evaluation.js --offline",
    "check:syntax": "node --check main.js && node --check server.js && node --check evaluation/run-evaluation.js && node --check src/triage-core.js",
    "test:unit": "node --test tests/unit/triage-core.test.js",
    "test:integration": "node --test tests/integration/analyze-endpoint.test.js",
    "test": "npm run test:unit && npm run test:integration",
    "docker:build": "docker build -t community-triage .",
    "docker:run": "docker run -p 3000:3000 --env-file .env.local community-triage"
  }
}
